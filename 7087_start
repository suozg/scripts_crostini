#!/bin/bash

python_script="$HOME/awards/drs/drs_wx.bin"
data_file="/mnt/chromeos/removable/SD Card/STATYSTYKA/NN/7087.db"

# Гарантуємо, що іконка зникне при закритті або збої скрипту
cleanup() {
    xsetroot -name ""
}
trap cleanup EXIT INT TERM

xsetroot -name "    "

if [ ! -f "$data_file" ]; then
    dunstify -u critical "Файл $data_file не знайдено."
    exit 1
fi
